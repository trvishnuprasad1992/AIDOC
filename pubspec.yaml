name: vertexaitesting
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ^3.5.0
dependencies:
  firebase_ai: ^2.0.0
  flutter:
    sdk: flutter
  google_ml_kit: ^0.20.0
  image_picker: ^1.1.2
  firebase_core: ^3.13.1
  permission_handler:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0



flutter:
  uses-material-design: true

